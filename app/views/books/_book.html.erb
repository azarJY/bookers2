<h2>User info</h2>
<div>
  <%= image_tag user.get_profile_image(100,100) %>
  <p>name: <%= user.name %></p>
  <p>introduction: <%= user.introduction %></p>
  <% if @user == current_user %>
    <p><%= link_to "edit仮", edit_user_path(user) %></p>
  <% end %>
</div>

<h2>New book</h2>

<%= form_with model: book do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Opinion</p>
  <%= f.text_area :body %>
  <%= f.submit 'Create BooK' %>
<% end %>